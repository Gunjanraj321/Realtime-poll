import React from 'react';
import CreatePoll from './CreatePoll';

const Body = () => {

    return (
        <div>
            <h1>Polling App</h1>
            <CreatePoll />
        </div>
    );
};

export default Body;
